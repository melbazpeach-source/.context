[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sirens-mcp-intelowl"
version = "0.1.0"
description = "MCP server wrapping IntelOwl"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2",
    "httpx>=0.27",
    "pydantic>=2.7",
    "structlog>=24.1",
]

[project.scripts]
sirens-mcp-intelowl = "sirens_mcp_intelowl.server:main"

[tool.hatch.build.targets.wheel]
packages = ["sirens_mcp_intelowl"]
