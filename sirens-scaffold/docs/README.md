# Sirens Docs

| File | Purpose |
|---|---|
| `ethics-and-legal.md` | Hard "MUST NOT" rules, authorization matrix, data minimization, disclosure policy |
| `scope-allowlist.template.yaml` | Per-engagement allow-list the supervisor enforces at runtime |

Operator runbooks live in `../runbooks/`. Architecture, build sequence, and
costs live upstream in `.context/research/sirens-*.md`.
